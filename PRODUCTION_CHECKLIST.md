# Micro-SaaS Scout Production Checklist

## Build outputs

- [x] `dashboard/dist` generated by `npm run build:dashboard`
- [x] `server/dist` generated by `npm run build:server`
- [x] `extension/dist` generated by `npm run build:extension:production`
- [x] `deploy/chrome/micro-saas-scout-extension.zip` generated by `npm run package:extension`

## Deployment configs

- [x] `vercel.json` configured for Vercel dashboard deployment
- [x] `render.yaml` configured for Render backend deployment
- [x] `dashboard/.env.production.example` created
- [x] `server/.env.production.example` created
- [x] `extension/.env.production.example` created

## Backend production readiness

- [x] Express server builds to `server/dist`
- [x] Health endpoint available at `/health`
- [x] CORS allows configured Vercel dashboard origin
- [x] CORS supports Chrome extension origins
- [x] CORS can be locked to `CHROME_EXTENSION_ID` after Chrome Web Store publishing
- [x] Rate limiting enabled
- [x] Helmet security headers enabled
- [x] Zod validation enabled
- [x] OpenAI, Gemini, Claude, and demo AI provider paths available
- [x] Supabase service role stays backend-only
- [x] Stripe backend keys stay backend-only

## Dashboard production readiness

- [x] Vite production build passes
- [x] Routes are lazy-loaded for smaller initial bundle
- [x] `VITE_API_URL` points to Render API placeholder
- [x] SPA rewrite configured in `vercel.json`
- [x] Dashboard output directory is `dashboard/dist`

## Chrome extension production readiness

- [x] Manifest V3 production build passes
- [x] Extension CSP uses HTTPS-only `connect-src` in production
- [x] Production API URL is embedded in built extension assets
- [x] Upload-ready zip exists at `deploy/chrome/micro-saas-scout-extension.zip`
- [x] Store listing template created at `docs/CHROME_STORE_LISTING.md`
- [x] Privacy policy template created at `docs/PRIVACY_POLICY.md`
- [x] Screenshot checklist created at `deploy/screenshots/README.md`

## Supabase readiness

- [x] SQL schema created at `supabase/schema.sql`
- [x] RLS policies included
- [x] Supabase setup guide includes exact SQL Editor, auth, storage, and RLS steps

## Final verification commands

The final readiness pass must run:

```bash
npm run typecheck
VITE_API_URL=https://micro-saas-scout-api.onrender.com npm run deploy:prepare
npm audit --audit-level=high
```

Expected result:

- typecheck passes
- deploy artifacts are generated
- audit reports 0 high vulnerabilities
